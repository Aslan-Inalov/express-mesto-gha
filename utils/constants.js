const JWT_SECRET = 'mysecretkey';

const ERROR_CODE = 400;
const ERROR_NOT_FOUND = 404;
const ERROR_SERVER = 500;

module.exports = {
  JWT_SECRET,
  ERROR_CODE,
  ERROR_NOT_FOUND,
  ERROR_SERVER,
};
